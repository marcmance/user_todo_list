<% @user.tasks.completed.each do |t| %>
    <tr>
      <td>
        <%= t.description %>
      </td>
      <td>
        <%=link_to "Delete", task_path(t), :method => :delete%>
      </td>

    </tr>
<%end%>