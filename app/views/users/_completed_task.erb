<% @user.tasks.completed.each do |t| %>
    <tr>
      <td>
        <%= t.description %>
      </td>
      <td>
        <%=link_to "Not Complete", uncomplete_task_path(t), :method => :put%> | <%=link_to "Delete", task_path(t), :method => :delete%>
      </td>

    </tr>
<%end%>